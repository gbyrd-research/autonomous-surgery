[project]
name = "autonomous-surgery"
version = "1.0.0"
requires-python = ">=3.9"

dependencies = [
    "torch>=2.1",
    "torchvision",
    "transformers",
    "datasets",
    "accelerate",
    "hydra-core",
    "wandb",
    "tqdm",
    "einops",
    "numpy",
]